# Analytics URL handling plan

Update the member fee registration page so analytics receives a normalized route instead of the unique registration path. Keep the registration flow working after normalization. Verify lint and build.
