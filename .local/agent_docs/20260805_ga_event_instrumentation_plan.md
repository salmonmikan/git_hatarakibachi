# GAイベント追加計画

## 対象

- 既存の `data-gtm-*` 属性方式を維持する。
- 高優先度のカルーセル操作、パフォーマンス画像拡大を追加する。
- 中〜低優先度のSNSメニュー開閉、ホーム導線、詳細ページの戻る導線、404復帰を追加する。
- 既存の未コミット変更はコミット対象から除外する。

## 確認方針

- `npm run lint` と `npm run build` を実行する。
- Lintで既存エラーと今回追加分のエラーを分離して報告する。
- ステージングはGA計測追加分だけに限定する。
