# GAイベント追加結果

## 実装内容

- ホームタイトルの内部リンクにナビゲーション計測属性を追加。
- Featuredカルーセルの前へ／次へ操作に `navigate` 属性を追加。
- 公演詳細のメイン画像・ギャラリー画像の拡大操作に `image_open` 属性を追加。
- 公演・記事・ニュース詳細のホーム戻るリンクに計測属性を追加。
- SNSフローティングメニューの開閉に `toggle` 属性を追加。
- 404ページのトップ復帰ボタンに計測属性を追加。

## 検証

- `npm run build`: 成功。
- `npm run lint`: 既存コードを含む30エラー・4警告で失敗。今回の属性追加に直接起因するエラーは確認されなかった。
- `git diff --check` / `git diff --cached --check`: 成功。

## コミット範囲

- GA計測追加分のみをステージし、既存の管理画面・表示修正は未ステージのまま残す。
- `page_view_custom` とGA4標準 `page_view` の二重送信有無はGTMコンテナ側の確認事項として残す。
